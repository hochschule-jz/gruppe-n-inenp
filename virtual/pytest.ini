[pytest]
# Make the two packages importable as top-level (`import traffic_light`,
# `import load_generator`) when running `pytest` from `virtual/` or `pytest virtual/`.
pythonpath = .
addopts = --import-mode=importlib
testpaths = traffic_light load_generator
